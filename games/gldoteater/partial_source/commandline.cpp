#include "main.h"

void MainWindow::HandleCommandLine( HINSTANCE hThisInstance, HINSTANCE hPrevInstance, 
                                    LPSTR lpszArgument,      int nFunsterStil )
{/*
   wxCmdLineParser cmdline;
   static const wxCmdLineEntryDesc cmdLineDesc[] =
   {
       { wxCMD_LINE_SWITCH, "v",     "verbose",    "be verbose"   },
       { wxCMD_LINE_SWITCH, "e",     "extract",    "extract file" },
       { wxCMD_LINE_SWITCH, "q",       "quiet",    "be quiet"     },
       { wxCMD_LINE_SWITCH, "f",  "fullscreen",    "fullscreen"   },

       { wxCMD_LINE_PARAM,  NULL, NULL, "port", wxCMD_LINE_VAL_NUMBER, wxCMD_LINE_PARAM_OPTIONAL },
       { wxCMD_LINE_PARAM,  NULL, NULL,   "ip", wxCMD_LINE_VAL_STRING, wxCMD_LINE_PARAM_OPTIONAL },

       { wxCMD_LINE_NONE }
   };
   cmdline.SetLogo( wxString( "gldoteater a pacman clone" ) );
   cmdline.SetDesc( cmdLineDesc );

   wxArrayString strings = cmdline.ConvertStringToArgs( lpszArgument );
//   wxString arg( lpszArgument );
//   Alert( lpszArgument );
   cmdline.SetCmdLine( strings );
   cmdline.Parse( false );
   if( cmdline.Found( wxString( "port" ) ) )
   {
   }*/
}
